//reverse a number
#include<stdio.h>
#include<conio.h>
int main()
{
	int n,rev=0,j;
	printf("enter the num");
	scanf("%d",&n);
	while(n!=0)
	{
	
		j=n%10;
		rev=rev*10+j;
		n=n/10;
	}
	printf("reverse order : %d\n",rev);
}
