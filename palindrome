//palindrome
#include<stdio.h>
#include<conio.h>
int main()
{
  	int n,k,rem,rev=0;
	  printf("enter the num");
	  scanf("%d",&n);
	  k=n;
	  while(n!=0)
	  {
	  	rem=n%10;
	  	rev=(rev*10)+rem;
	  	n=n/10;
		  }
		  if(k==rev)
		  printf("palindrome");
		  else
		  printf("not palindrome");	
}
